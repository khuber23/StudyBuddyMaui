<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="NtcMaui.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:views="clr-namespace:NtcMaui.Views.SignAndCreate"
    Shell.FlyoutBehavior="Disabled">
<!---Eventually have this be sign in, for now just testing the flashcard flip so change it back to sign in eventually
    And the namespace back to NtcMaui.Views.SignAndCreate-->
    <ShellContent
        ContentTemplate="{DataTemplate views:SignIn}" Route="Sign in"/>

</Shell>
